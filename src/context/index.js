import { createContext } from "react";

const Context = createContext(null)

export default Context;


//Create a Context: Use React.createContext() to create a context object. This object includes a Provider and Consumer.